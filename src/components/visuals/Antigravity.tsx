import React from 'react';
import CssNebulaFallback from './CssNebulaFallback';

// Shim for Antigravity (R3F removed)
// Returns the CSS Nebula Fallback for visual continuity
export default function Antigravity(props: any) {
    return <CssNebulaFallback />;
}
